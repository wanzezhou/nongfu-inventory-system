# DESIGN.md — 进销存系统设计规范（红墨留白·克制版）

> 本文件是所有前端改动的**视觉基本法**：AI/人工改动任何页面前必读，改完自查。
> 基底：现有「红墨留白·沉稳版」token 体系；方法论：吸收 Linear/Vercel 中后台设计纪律 + 反 AI 味硬禁令。

## 1. 视觉主题

中后台工具，第一原则是**信息优先于装饰**。风格关键词：留白、克制、单一强调色。水墨/衬线只作点缀（页面标题、Logo），不侵入数据区。

## 2. 语义化配色（唯一来源：`frontend/src/style.css` :root token）

- 背景 `--bg: #F5F4F0`（暖灰），卡片 `--card: #FFFFFF`，边框 `--border: #E8E6DF`
- 文字三级：`--text #1F1F1F` / `--text-2 #7A7A72` / `--text-3 #AEAEA5`
- 强调色唯一：品牌红 `--primary #A8201A`；辅助语义色仅 `--green`（增/正向）、`--gold`（警示/欠款）
- **硬禁令**：
  - 视图层禁止硬编码 hex/rgb 色值，一律走 token（含 SVG fill、ECharts 配置、内联样式）
  - 禁止装饰性渐变（三色彩虹线、紫蓝 hero 渐变、浮动光斑）；渐变仅允许两处：折线下方面积的单色淡出、Logo 底部 1px 分隔线
  - 同一页面强调色不超过 1 种 + 语义色不超过 2 种

## 3. 字体层级

- 正文 `--font-sans`（Noto Sans SC）；仅页面主标题/品牌名/仪表盘大数字用 `--font-serif`，**表格、表单、按钮、正文一律 sans**
- 字号阶梯固定 5 档：12（辅助）/ 13（正文-表格）/ 14（正文）/ 16（区块标题）/ 22-34（页面级数字，衬线）
- 一屏内字号档位不超过 5 种；禁止无层级的小字海洋

## 4. 组件样式

- Element Plus 品牌色已全局覆盖（style.css），页面内不再单独定制同款组件
- 表格密度：默认尺寸、斑马纹可选、行高不额外放大；操作列固定右侧
- 标签/徽章必须对应真实状态或真实数据，**禁止「实时」「智能」等无逻辑修饰词**

## 5. 布局与间距

- 8px 网格：所有 margin/padding/gap 取 4 的倍数，卡片内边距 20-24px，页面内容区 28px
- 卡片圆角统一 `--radius-md 10px`（大卡 `--radius-lg 14px`），禁止混用 6/12/18/24px
- 统计卡区不做三等宽炫技：卡片数量由**真实指标**决定（4-6 张），`el-col` 必须带 `:xs=24 :sm=12 :md=x` 响应式

## 6. 阴影与层级

- 静态：`--shadow-sm`（卡片默认）；弹层/下拉：`--shadow-md`
- **禁令**：hover 位移 + 彩色阴影 + 放大缩放三件套不再默认使用；卡片 hover 只允许 `border-color` 或阴影轻微加深，不加 `transform`

## 7. 动效

- 全局禁用无限循环装饰动画（浮动圆、logo pulse、发光呼吸）；唯一例外为登录页动态背景（§10）
- 入场动画统一为一种：`fadeUp 0.4s ease-out`（opacity + translateY(8px)），禁用 blur、scale 过冲、弹簧缓动
- 交互动效仅保留：按钮按压（0.1s）、下拉展开；页面切换 fade ≤ 0.25s

## 8. 数据真实性（去 AI 味核心）

- **禁止编造数据**：趋势百分比、环比、进度条宽度必须有真实来源（后端接口计算），算不出就不显示
- 接口失败的降级策略是**显示空态/错误提示**，不是随机数、不是硬编码假值
- 图表配色：单一系列用 `--primary` 实线；多系列用语义色区分，禁止彩虹渐变线

## 9. 响应式与自查清单（每次前端改动跑一遍）

- [ ] ≤768px：对话框 94vw、卡片换行堆叠、长表单 body 限高滚动
- [ ] 无新增硬编码色值；无新增装饰动画/渐变（登录页动态背景除外，见 §10）
- [ ] 展示的每个数字都能追溯到接口字段；失败分支不误报成功
- [ ] 字号档位未超标；圆角走 token；hover 无 transform 三件套
- [ ] 完成后同步 `docs/项目概览.md` 变更记录

## 10. 唯一例外：登录页动态背景

登录页是**唯一的门面页**，不承载数据，允许突破 §2「禁渐变」与 §7「禁无限动画」，但有边界：

- **仅限 `views/Login.vue` 及其专用的 `components/InkBackdrop.vue`**，其余任何页面不得引用该组件
- 视觉母题固定为「**水墨山泉**」：墨团晕染 + 水面涟漪 + 山泉滴落，冷色/科技感粒子不在此列
- 配色仍**只能取自 :root token**（运行时 `getComputedStyle` 读取），不得出现字面 hex
- 表单区（输入框、按钮、文字）仍走常规规范：白底实心、token 着色、无玻璃拟态模糊
- 必须实现：`prefers-reduced-motion` 降级为静态一帧、页面隐藏暂停 rAF、DPR 上限 2、窄屏降低密度

> 新增动态/Demo 页面前先问一句：它是门面页吗？不是就走常规规范。

## 参考对齐

Linear（克制配色/字号纪律）、soybean-admin-element-plus（同栈主题实现）、Geeker-Admin（列表页布局范式）。详见资料库《前端UI美化与去AI味开源项目调研报告》。
