# DESIGN.md — 进销存系统设计规范（中性极简 · 对齐 WorkBuddy）

> 本文件是所有前端改动的**视觉基本法**：改动任何页面前必读，改完自查。
> 2026-09-17 由「红墨留白·沉稳版」重写为「中性极简」：去掉装饰，让信息本身成为主角。

## 1. 视觉主题

中后台工具，第一原则是**信息优先于装饰**。风格关键词：**无色系基底、单一强调色、零装饰、充足留白**。
参照 WorkBuddy / Linear / Vercel 的中后台纪律：**不用渐变、不用毛玻璃、不用位移与缩放动画、不用彩色阴影**。

## 2. 语义化配色（唯一来源：`frontend/src/style.css` :root token）

- **底色三级**：`--page-bg #FFFFFF`（页面底）/ `--bg #F7F7F7`（卡片内部次级浅底：表头、弹窗头尾、分区、胶囊）/ `--card #FFFFFF`
  - 判断用哪个：**「这是页面底吗？」是 → `--page-bg`；否 → `--bg`**
  - 页面与卡片同为白底 ⇒ **卡片可辨识度靠 `--border` 边框**（`--shadow-sm` 在白底上几乎不可见），新增白底卡片必须带边框
- **文字三级**：`--text #171717` / `--text-2 #6B6B6B` / `--text-3 #9B9B9B`（中性灰阶，**不得带暖黄调**）
- **描边**：`--border #E6E6E6`、`--border-strong #D4D4D4`（hover/次级强调）
- **强调色唯一**：品牌红 `--primary #A8201A`。**只允许出现在三处**：
  1. 主按钮（实心填充）
  2. 激活/选中态（侧边栏菜单项、页签指示条、单选框、分页当前页）
  3. 语义状态（危险/欠款/负向数字）
- **语义色**：`--green`（成功/正向）、`--gold`（警告），以及 Element 的 danger 红
- **硬禁令**：
  - 视图层禁止硬编码 hex/rgb，一律走 token（含 SVG fill、ECharts 配置、内联样式）
  - 禁止装饰性渐变（`body` 上原有的两层径向色斑已于 2026-09-17 移除）
  - 禁止毛玻璃（`backdrop-filter`）、禁止彩色阴影
  - 同一页面强调色不超过 1 种 + 语义色不超过 2 种；**禁止出现第二种「品牌色」**

## 3. 字体层级

- 正文 `--font-sans`（Noto Sans SC）。**除品牌标识外一律 sans**：表格、表单、按钮、弹窗标题、面包屑、仪表盘数字全部 sans
- `--font-serif` 仅保留给 Logo 文字（品牌标识，见 `main-layout.css` 的 `.logo-text`）
- 字号阶梯固定 5 档：12（辅助）/ 13（正文-表格）/ 14（正文）/ 15-16（区块与弹窗标题）/ 22-34（页面级数字）
- 一屏内字号档位不超过 5 种；禁止无层级的小字海洋

## 4. 组件样式（Element Plus 全局覆盖，页面内不再单独定制同款组件）

| 组件 | 规范 |
|---|---|
| 按钮 | 圆角 `--radius-md`；交互只改背景/边框色（**无扫光、无位移、无缩放、无彩色阴影**）；主按钮实心红；默认按钮白底细边框；**文字按钮中性灰**，仅危险操作用红 |
| 卡片 | 静态 1px 边框 + `--shadow-sm`；hover **只加深边框**，不上浮、不加阴影、不位移 |
| 表格 | 表头 `--bg` 底、12px、500 字重、`--text-2` 色、无字距；行 hover 中性 `--bg`（**不用彩色底**）；操作列常显，不要「hover 才显形」 |
| 输入/选择 | 圆角 `--radius-md`；focus **只留 1px 主色描边**（无光晕、无位移） |
| 弹窗 | 圆角 `--radius-xl 12px`；实白 + `--shadow-lg`（**无毛玻璃**）；头部/底部白底 + 1px 分隔线（无渐变）；标题 15px/600 sans；入场仅 0.16s 淡入 + 6px 位移 |
| 下拉/消息 | 实白 + `--shadow-md`；无毛玻璃、无弹跳动画 |
| 标签 | 圆角 `--radius-sm`；默认中性灰底灰字；语义色仅用于真实状态；无 hover 位移 |
| 分页 | 当前页实心红；无位移、无阴影 |

- 标签/徽章必须对应真实状态或真实数据，**禁止「实时」「智能」等无逻辑修饰词**
- 表格密度：默认尺寸、行高不额外放大；操作列固定右侧

## 5. 布局与间距

- 8px 网格：margin/padding/gap 取 4 的倍数；卡片内边距 16-20px；**页面内容区 20px**（窄屏 12px）
- 卡片圆角统一 `--radius-lg 10px`（弹窗 12px），禁止混用
- 侧边栏：白底 + 1px 右边框；菜单项高 38px、圆角 8px、**hover 灰底 / 激活浅红底 + 红字**（不画左侧竖条）
- 顶栏：白底 + 1px 底边（无毛玻璃），高 56px；面包屑 sans 13px，末级深色、非末级灰色
- 统计卡区不做三等宽炫技：卡片数量由**真实指标**决定，`el-col` 必须带 `:xs=24 :sm=12 :md=x`

## 6. 阴影与层级

- 静态：`--shadow-sm`（卡片默认）；弹层/下拉：`--shadow-md`；弹窗：`--shadow-lg`
- **禁令**：hover 位移、缩放、彩色阴影**一律不允许**；卡片 hover 只允许 `border-color` 加深

## 7. 动效

- 全局禁用无限循环装饰动画；交互动效只保留：背景/边框色 0.15s、下拉展开、页面切换 0.18s 淡入
- 入场动画统一为 `fadeUp 0.24s ease-out`（opacity + translateY(6px)）；**禁用 blur、scale、弹簧缓动**
- 唯一例外：登录页动态背景（§10）

## 8. 数据真实性（去 AI 味核心）

- **禁止编造数据**：趋势百分比、环比、进度条宽度必须有真实来源（后端接口计算），算不出就不显示
- 接口失败的降级策略是**显示空态/错误提示**，不是随机数、不是硬编码假值
- 图表配色：单一系列用 `--primary` 实线；多系列用语义色区分，禁止彩虹渐变线

## 9. 响应式与自查清单（每次前端改动跑一遍）

- [ ] ≤768px：对话框 94vw、卡片换行堆叠、长表单 body 限高滚动
- [ ] 无新增硬编码色值；无渐变/毛玻璃/位移缩放动画
- [ ] 强调色只出现在主按钮、激活态、语义状态三处
- [ ] 展示的每个数字都能追溯到接口字段；失败分支不误报成功
- [ ] 字号档位未超标；圆角走 token
- [ ] **跑验证**：`cd frontend && npm run ui-smoke`（真实浏览器断言，含底色/去装饰/悬浮按钮 28 项）
- [ ] 需要目视比对时：`node frontend/scripts/ui-shot.mjs --out .ui-shots/after`（截图写入 `.ui-shots/`，已 gitignore）
- [ ] 完成后同步 `docs/项目概览.md` 变更记录

## 10. 唯一例外：登录页动态背景

登录页是**唯一的门面页**，不承载数据，允许突破 §2「禁渐变」与 §7「禁无限动画」，但有边界：

- **仅限 `views/Login.vue` 及其专用的 `components/InkBackdrop.vue`**，其余任何页面不得引用该组件
- 视觉母题固定为「**水墨山泉**」：墨团晕染 + 水面涟漪 + 山泉滴落，冷色/科技感粒子不在此列
- 配色仍**只能取自 :root token**（运行时 `getComputedStyle` 读取），不得出现字面 hex
- 表单区（输入框、按钮、文字）仍走常规规范：白底实心、token 着色、无玻璃拟态模糊
- 必须实现：`prefers-reduced-motion` 降级为静态一帧、页面隐藏暂停 rAF、DPR 上限 2、窄屏降低密度

## 参考对齐

WorkBuddy（中性无色系 / 零装饰 / 精简外壳）、Linear（克制配色/字号纪律）、soybean-admin-element-plus（同栈主题实现）。

## 附：本次重构（2026-09-17）改动清单

| 维度 | 改动前 | 改动后 |
|---|---|---|
| 底色 | 暖灰 `#F5F4F0`（页面底与浅底共用一个 token） | 页面纯白 `--page-bg`；浅底中性灰 `--bg #F7F7F7`（拆 token） |
| 文字/边框 | 暖黄调（`#7A7A72`/`#E8E6DF`） | 中性（`#6B6B6B`/`#E6E6E6`） |
| 圆角 | 6/10/14/20 | 6/8/10/12 |
| 按钮 | 扫光 + hover 上移 + 缩放 + 彩色阴影 | 仅背景/边框色变化 |
| 卡片 | hover 上浮 2px + 大阴影 + 红边框 | hover 只加深边框 |
| 表格 | hover 红色淡底；操作列 hover 才显形 | hover 中性灰；操作列常显；操作文字中性灰 |
| 弹窗 | 20px 圆角 + 毛玻璃 + 弹簧动画 + 头部渐变 | 12px + 实白 + 0.16s 淡入 + 白底头尾 |
| 输入框 | focus 4px 红色光晕 + 上移 | focus 仅 1px 描边 |
| 外壳 | 顶栏毛玻璃、菜单项 46px + 左侧红竖条、内容区 28px | 顶栏实白 56px、菜单项 38px 圆角块、内容区 20px |
| 字体 | 弹窗标题/面包屑/仪表盘大数字用衬线 | 除 Logo 外全部 sans |
