# 农富库存管理系统 — 一键部署说明

> 目标：把本项目复制到**任何一台 Windows 电脑**，双击 `start.bat` 即可运行，无需手动配置数据库和依赖。

---

## 一、环境要求（目标电脑需预装）

| 软件 | 版本 | 说明 |
|------|------|------|
| **Node.js** | 18+（推荐 20 LTS） | https://nodejs.org/ 下载安装，安装时勾选「Add to PATH」 |
| **MySQL** | 5.7+ / 8.0 | 安装并启动服务（Windows 服务名 MySQLxx），root 账号 |
| 浏览器 | Chrome / Edge 均可 | 自动打开前端页面 |

> 目标电脑**不需要**预装 Git、npm 全局包、任何编程环境，只需上述两件。

---

## 二、文件说明（本项目内）

| 位置 | 文件 | 作用 |
|------|------|------|
| 项目根目录 | `start.bat` | **一键启动脚本**（双击即可） |
| `database/` | `full_schema_data.sql` | 全量数据库（27 张表结构 + 演示数据），随项目分发 |
| `backend/scripts/` | `init_db.js` | 新电脑数据库初始化（幂等：已初始化则跳过） |
| `backend/scripts/` | `export_dump.js` | 重新导出 `full_schema_data.sql`（数据更新后使用） |
| `backend/` | `.env.example` | 后端环境变量模板（首次启动自动复制为 `.env`） |

---

## 三、部署步骤

1. **安装 Node.js 与 MySQL**（见上表），启动 MySQL 服务。
2. **复制整个项目文件夹**到目标电脑任意目录（如 `D:\nongfu`）。
3. 打开 `start.bat`，按需修改顶部**配置区**：
   - `DB_PASSWORD`：MySQL root 密码（无密码留空）
   - `NPM_REGISTRY`：npm 镜像源（默认国内 npmmirror，可留空用官方源）
4. **双击 `start.bat`**，脚本会自动：
   - 检测 Node.js / MySQL
   - 初始化数据库（建库 + 建表 + 演示数据，仅首次）
   - 安装后端、前端依赖（仅首次，需几分钟）
   - 启动后端（3000）与前端（5173）
   - 自动打开浏览器 `http://localhost:5173`
5. **登录系统**：账号 `admin`，密码为该账号在 `users` 表的哈希值（演示数据默认沿用导出时账号）。

> 数据库已初始化后再次启动会**跳过**初始化，不会覆盖已有数据。

---

## 四、常见问题

| 现象 | 处理 |
|------|------|
| 提示「未检测到 Node.js」 | 安装 Node.js 并重新打开 cmd 再运行 |
| 提示「未检测到 MySQL」 | 确认 MySQL 服务已启动（服务管理器中启动 MySQLxx） |
| 初始化失败：无法连接 MySQL | 在 `start.bat` 顶部填对 `DB_PASSWORD` |
| 依赖安装慢/失败 | 换镜像：把 `NPM_REGISTRY` 改为 `https://registry.npmmirror.com/` 或官方源重试 |
| 端口 3000/5173 被占用 | 关闭占用程序，或在 `backend/.env` 改 `PORT`、`frontend/vite.config.js` 改端口 |
| 想重置数据库 | 手动删除 MySQL 中 `nongfu_inventory` 库后重新运行 `start.bat` |

---

## 五、数据导出（维护人员）

业务数据变化后，重新生成分发包用的全量 SQL：

```bash
node backend/scripts/export_dump.js [root密码]
# 输出 database/full_schema_data.sql，随项目一起分发
```
