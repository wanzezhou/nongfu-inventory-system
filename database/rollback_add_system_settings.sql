-- ============================================================================
-- 回滚：删除系统配置表 system_settings（撤销 2026-09-16 的 migration_add_system_settings.sql）
-- ----------------------------------------------------------------------------
-- ⚠️ 该表回滚会一并丢弃其中所有配置项（当前仅 print_manager_worker_id）。
--    回滚后「销售单打印店长」将回退为「第一位启用的店长」规则，功能不中断。
-- 幂等：DROP TABLE IF EXISTS
-- ============================================================================

DROP TABLE IF EXISTS `system_settings`;
