新增财务模块：按订单类型统计财务指标

后端（/api/finance）
- financialController.js + financialRoutes.js：
  - GET /summary 按订单类型分组聚合 5 类指标（线上平台销售=总包配送费、
    线下水站分销=分销价、线下零售=零售价、量贩机供货=工人配送费、
    线下水站返货=进货价+分销配送费），金额=单价(order_items)×数量，排除已取消
  - GET /orders 逐单明细分页；GET /orders/export 一键导出 xlsx
- 修复 mysql2 预处理不支持 LIMIT/OFFSET 占位符：与 orderController 一致内联
  LIMIT 与 OFFSET 整型值

前端
- 新建 api/finance.js、views/finance/Finance.vue（finance 空目录已存在）
- 路由 /finance + 菜单「财务管理 > 财务明细统计」（Money/Wallet 图标）
- 页面：日期范围+订单类型多选筛选；指标汇总卡片+合计；逐单明细表
  （按类型展示对应指标，返货显示进货价+分销配送费）；导出；窄屏适配
- 修复图标：@element-plus/icons-vue 无 Truck，改用 Van

验证：后端语法+模块加载 OK；summary/orders/export 冒烟 200；前端 vite build OK
文档：docs/项目概览.md 同步（API 表/前端视图/报表口径/文件索引）
