/* 主数据编辑（Phase 8b 第 4~7 域）
   本页几乎所有样式都走 app.wxss 的全局 form-* 类（四域共用一套表单外观）；
   这里只放「字段级提示文字」的微调。 */

/* 字段下方的 hint 与上一个输入行贴得近一点，避免看起来像下一个字段的标签 */
.form-row + .t-sub {
  margin-top: -8rpx;
  margin-bottom: 16rpx;
}
