<!-- 充值（Phase 6 未开通，如实说明 + 替代路径） -->
<view class="page">

  <view class="card">
    <view class="row-between">
      <text class="t-sub">当前积分</text>
      <text class="t-strong t-lg">{{balance === null ? '—' : balance}}</text>
    </view>
    <view class="t-sub mt-8">1 元 = 1 积分；积分用于订货支付</view>
  </view>

  <view wx:if="{{probeDone && !open}}" class="banner banner-warn">
    <view class="t-strong">微信充值暂未开通</view>
    <view class="mt-8">
      原因：微信支付资质尚未就绪（文档 §43 · Phase 6 是唯一必须等资质的阶段）。
      系统不会写入任何「已充值」的测试流水，以免污染真实账目（§13.8）。
    </view>
  </view>

  <view wx:if="{{probeDone && open}}" class="card">
    <view class="section-title">充值金额（元）</view>
    <view class="form-row">
      <view class="form-label">金额</view>
      <input class="form-input" type="digit" placeholder="请输入充值金额" value="{{amount}}" bindinput="onAmountInput" />
    </view>
    <button class="btn btn-primary btn-block mt-24" bindtap="onRecharge">微信支付充值</button>
  </view>

  <view class="card">
    <view class="section-title">当前可用的正向入账方式</view>
    <view class="way-row">
      <view class="way-idx">1</view>
      <view class="flex-1">
        <view class="t-strong">管理员手工调增积分</view>
        <view class="t-sub mt-8">
          需填写操作原因与备注，生成 ADJUST_IN 流水并留审计（文档 §18 / §40）。
          请线下联系管理员在管理端操作。
        </view>
      </view>
    </view>
    <view class="way-row">
      <view class="way-idx">2</view>
      <view class="flex-1">
        <view class="t-strong">水票发行产生的分销配送费积分</view>
        <view class="t-sub mt-8">
          仅直营水站适用；该链路属 Phase 7，本期未接入（数据库中单件值/总额列已按 §20.4 预置）。
        </view>
      </view>
    </view>
    <view class="way-row">
      <view class="way-idx">3</view>
      <view class="flex-1">
        <view class="t-strong">订单退款回补</view>
        <view class="t-sub mt-8">
          取消或退款时，原订单实际扣除的积分会全额退回原钱包（文档 §15.1 / §15.3）。
        </view>
      </view>
    </view>
  </view>

  <view class="card card-flat">
    <view class="section-title">为什么不做一个「模拟充值」按钮</view>
    <view class="t-sub">
      文档 §41 明令禁止把不存在的资金能力做成看起来可用的入口；§13.8 也明确
      「降级期间不要造假数据」。伪造的成功提示会让账实不符且无人察觉 ——
      这正是本仓库历史上踩过的坑（假数据兜底、失败却报成功）。
    </view>
  </view>

  <view class="card" bindtap="goTransactions">
    <view class="row-between">
      <text class="t-strong">查看积分流水</text>
      <text class="t-sub">›</text>
    </view>
  </view>
</view>
