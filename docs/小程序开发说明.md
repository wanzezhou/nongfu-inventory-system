# 微信订货小程序 · 开发说明（V1.1 · Phase 1~5 + 8a）

> 事实源：[《农夫库存管理系统微信订货小程序 产品需求、业务规则与技术开发文档 V1.1》](https://www.workbuddy.cn/space/d/eHWlowb5KBGk85OWvfj4xb)（下文引用一律写作「§N 节」）
> 交付日期：2026-09-20　｜　交付范围：业务员端 + 直营水站端全量 + 管理员只读（含钱包后台）
> 阅读对象：接手开发、联调、验收人员

---

## 一、这份交付包含什么 / 不包含什么

### 1.1 已交付（对应文档 §43 分阶段计划）

| 阶段     | 内容                                                                                                                                                | 状态                                         |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| Phase 1  | 账号与框架：`wx.login` → openid → 手机号绑定 → 签发小程序令牌；`mini_accounts` 清理与唯一约束                                                       | ✅                                           |
| Phase 2  | 商城：商品列表 / 搜索 / 分类 / 商品详情 / 购物车                                                                                                    | ✅                                           |
| Phase 3  | 业务员订单：客户信息、**成交价（前端可填）**、最低价校验、历史成交价、配送（**自提已于 2026-09-20 下线**）、`order_type=3`、钱包支付、`order_scene` | ✅                                           |
| Phase 4  | 直营水站订单：`order_type=2`、服务端强制分销价、水票抵扣（含部分抵扣）、不产生欠款                                                                  | ✅                                           |
| Phase 5  | 钱包：`wallet_accounts` / `wallet_transactions`、积分消费、退款、管理员调整（强制原因）                                                             | ✅                                           |
| Phase 8a | 管理员移动后台 · **只读**：仪表盘 / 待办 / 预警 / 水站积分总额 / 待退款                                                                             | ✅                                           |
| Phase 6  | 微信充值                                                                                                                                            | ❌ **未做**（见 §5.1）                       |
| Phase 7  | 水票积分（发行入账 / 作废回冲）                                                                                                                     | ❌ **未做**（见 §5.2）                       |
| Phase 8b | 管理员写操作全覆盖（17 业务域，**逐域验收**）                                                                                                       | 🔶 **进行中**：1/17（支出/费用 ✅，见 §7.3） |

### 1.2 页面清单（15 个，逐项对照文档 §5）

| #   | 页面路径                          | 对应文档            | 说明                                                                        |
| --- | --------------------------------- | ------------------- | --------------------------------------------------------------------------- |
| 1   | `pages/login/index`               | §4.4                | 微信登录 / 手机号绑定 / 本地开发登录                                        |
| 2   | `pages/home/index`                | §29 §30 §28         | 角色分流首页：业务员 / 水站 / 管理员仪表盘                                  |
| 3   | `pages/mall/index`                | §5.1 §5.2 §35       | 商城（可售口径按渠道区分）                                                  |
| 4   | `pages/product-detail/index`      | §29 §30             | 商品详情 + 可用水票数                                                       |
| 5   | `pages/cart/index`                | §5.1 §5.2 §9.2      | 购物车 + 水票抵扣选择                                                       |
| 6   | `pages/order-confirm/index`       | §7 §8 §10 §14 §23.1 | 确认订单 / **成交价可填** / 最低价提示 / 幂等提交（**仅配送**，自提已下线） |
| 7   | `pages/order-list/index`          | §31                 | 订单列表（筛选按角色不同）                                                  |
| 8   | `pages/order-detail/index`        | §32                 | 订单详情（16 项 + 最低价审计）                                              |
| 9   | `pages/wallet/index`              | §33                 | 积分钱包                                                                    |
| 10  | `pages/wallet-transactions/index` | §33 §11.4           | 积分流水                                                                    |
| 11  | `pages/wallet-recharge/index`     | §13 §13.8           | 充值（Phase 6 未开通，如实说明）                                            |
| 12  | `pages/station-tickets/index`     | §30                 | 我的水票                                                                    |
| 13  | `pages/station-profile/index`     | §5.2 §10.3          | 我的水站（只读）                                                            |
| 14  | `pages/mine/index`                | §5.1 §5.2           | 我的 + 身份能力说明                                                         |
| 15  | `pages/admin-wallet/index`        | §18 §21.8           | 管理员积分钱包后台（查看 + 手工增减）                                       |
| 16  | `pages/admin-expenses/index`      | §5.3 §43            | **Phase 8b** 支出台账列表（区间/关键词筛选、合计、进出编辑）                |
| 17  | `pages/admin-expense-edit/index`  | §5.3 §43            | **Phase 8b** 支出新增/编辑/删除（幂等提交、账户可留空=只登记不动账）        |

> **覆盖度**：文档 §5.1 业务员 11 页 → 全覆盖；§5.2 直营水站 13 页 → 全覆盖；§28 管理员首页 → 全覆盖。
> **§5.3 管理员**：8a 只读已全覆盖；8b 写操作按 §43 逐域开通，当前 1/17（支出/费用）。

---

## 二、目录结构

```
miniprogram/                       # 微信小程序工程（原生 WXML/WXSS/JS）
├── app.js / app.json / app.wxss   # 入口与全局配置
├── project.config.json            # 微信开发者工具项目配置
├── sitemap.json
├── config/index.js                # ★ 编译期常量单一来源（角色/枚举/存储键/筛选集）
├── utils/
│   ├── request.js                 # ★ 统一请求层（令牌/401/滑动续期/错误提示）
│   ├── auth.js                    # 登录态与身份（权限由服务端下发）
│   ├── cart.js                    # 购物车（只存数量与水票选择，**不存价格**）
│   ├── idempotency.js             # ★ 幂等键（§23.1 三条硬要求的落地）
│   ├── format.js                  # 展示格式化（不做业务计算）
│   └── ui.js                      # tabBar 同步 / 登录守卫 / 二次确认
├── custom-tab-bar/                # 自定义 tabBar（按角色渲染不同 tab 集）
├── pages/                         # 15 个页面（见 §1.2）
└── scripts/
    ├── check-project.js           # 静态门禁：结构 / 事件函数 / wx:key / 密钥红线
    └── check-api-paths.js         # 静态门禁：小程序调用路径 ↔ 后端路由表

backend/src/
├── constants/mini.js              # ★ 小程序枚举与常量单一来源（DB 用 varchar 收口在这里）
├── middleware/
│   ├── miniAuth.js                # ★ 小程序鉴权（独立密钥 + 每请求查库校验 status）
│   └── auth.js                    # 【改】显式拒绝小程序令牌
├── services/
│   ├── walletService.js           # ★ 钱包核心（§11.7 七条铁律的唯一落地点）
│   ├── walletSummary.js           # ★ 对账取数单源（§53）
│   ├── miniOrderService.js        # ★ 订单编排（只编排，不复制核心逻辑）
│   ├── miniAccountService.js      # 登录 / 绑定 / 令牌签发
│   └── wxMiniClient.js            # 微信开放接口客户端（code2session / 手机号）
│     （原 miniSettings.js 只做「自提地点解析」，随自提下线整体删除）
├── controllers/mini/              # auth / catalog / order / wallet / waterTicket / home / admin
├── routes/miniRoutes.js           # ★ 25 条路由（顺序敏感，见 §3.2）
└── utils/orderIdGen.js            # 订单号生成（与 Web 共用，避免主键碰撞）

database/
├── migration_mini_program_v1.sql  # ★ 权威 DDL（17 个 STEP，带 GUARD 标记；含自提退役步骤）
└── backup_20260920_203139_before_miniprogram.sql
```

---

## 三、如何运行

### 3.1 启动后端（必须先启）

```bash
cd backend
node src/app.js
# 服务在 http://localhost:3000；探活用 GET /health（免鉴权、固定 200）
```

`backend/.env` 需要以下新增项（已写入本机 `.env`，`.env.example` 也已登记）：

```ini
JWT_SECRET_MINI=<64 位十六进制随机串>   # ⚠️ 必须与 JWT_SECRET 不同；缺失则后端拒绝启动
MINI_TOKEN_TTL=2h                      # 小程序令牌有效期
WX_APPID=                              # 微信登录用；留空则只能用本地开发登录
WX_SECRET=
MINI_DEV_LOGIN=1                       # ⚠️ 仅本地开发；生产必须删除或置 0
```

### 3.2 启动小程序

1. 微信开发者工具 → 导入项目 → 目录选择仓库下的 `miniprogram/`。
2. **AppID**：资质未就绪时可先用「测试号 / 无 AppID」（`project.config.json` 里是 `touristappid`）。
3. **详情 → 本地设置**：勾选「不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书」
   （因为 `config/index.js` 的 `API_ORIGIN` 是 `http://localhost:3000`）。
4. 编译后进入登录页。

### 3.3 首次登录怎么走

| 场景                            | 走法                                                                  |
| ------------------------------- | --------------------------------------------------------------------- |
| 已配置 `WX_APPID` / `WX_SECRET` | 点「微信一键登录」→ 未绑定时按提示授权手机号 → 服务端按手机号匹配身份 |
| 未配置微信凭据（当前默认）      | 登录页会提示，并可在「本地开发登录」里输入后台维护的手机号登录        |

**本地开发登录说明**（`POST /api/mini/auth/dev-login`，需 `MINI_DEV_LOGIN=1`）：

- 仍然要求手机号能匹配到**真实存在**的业务员 / 直营水站 / 管理员 —— 不创建任何假数据；
- 不允许自由选角色：`role` 只用于在**已匹配到的多条真实记录**里消歧
  （现状库中 `users.phone` 与 `sub_stations.phone` 确有重号，属真实数据特征）；
- 生产环境必须关闭。

**可用示例手机号**（取自当前库）：

| 身份     | 手机号        | 备注                                                                             |
| -------- | ------------- | -------------------------------------------------------------------------------- |
| 业务员   | `13600000001` | SM001 赵敏（`workers.employee_type=3`）                                          |
| 直营水站 | `13900000002` | ST002 秦淮水站                                                                   |
| 管理员   | `13900000001` | 该号同时命中 `users`(admin) 与 `sub_stations`(ST001)，需在登录页选「管理员」消歧 |

> ⚠️ 钱包初始为 0。下单前需先用**管理员身份**登录 → 「我的 → 积分钱包后台」→ 对目标钱包
> 「增加积分」（这是 §13.8 降级路径下唯一的正向入账来源）。

---

## 四、接口契约

统一约定（后端既有规范，小程序侧严格对齐）：

- 前缀 `/api/mini`；响应信封 `{ code, message, data }`，HTTP 状态码与 `code` **双写**。
- **鉴权失败 = HTTP 401 + code 401**（小程序请求层在 `fail`/error 分支处理，不只看 `response.data.code`）。
- 无权限 = HTTP 403。
- 请求体**一律驼峰**（后端全局 `normalizeBody` 做蛇形→驼峰并删原键）。
- 令牌滑动续期：令牌过半生命周期时，响应头 `X-Mini-Token` 下发新令牌。

### 4.1 认证（§21.1）

| 方法 | 路径              | 鉴权             | 说明                                                                                                                              |
| ---- | ----------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| GET  | `/config`         | 公开             | 公开配置：`devLoginEnabled` / `wxLoginConfigured` / `tokenTtl` / `messages`（**不再含 `pickupConfigured`**）                      |
| POST | `/auth/login`     | 公开             | `{ code, phoneCode?, nickname?, avatarUrl? }` → `{ status:'OK', token, account }` 或 `{ status:'NEED_BIND', bindTicket, reason }` |
| POST | `/auth/bind`      | 公开             | `{ bindTicket, phoneCode }` → `{ token, account }`                                                                                |
| POST | `/auth/dev-login` | 公开（env 门禁） | `{ phone, role? }` → `{ token, account, devLogin:true }`                                                                          |
| POST | `/auth/logout`    | miniAuth         | 记审计；令牌无状态，实际登出由前端清 token                                                                                        |
| GET  | `/me`             | miniAuth         | `{ account, subject, wallet, blocked, role, permissions }`（**不再含 `pickup`**）                                                 |

### 4.2 商城（§21.2）

| 方法 | 路径                   | 说明                                                      |
| ---- | ---------------------- | --------------------------------------------------------- |
| GET  | `/products`            | `?keyword&category&page&pageSize`（pageSize 上限 50）     |
| GET  | `/products/categories` | 全量分类（**专用全量接口**，不走分页）                    |
| GET  | `/products/:id`        | 详情；水站额外返回 `availableTicketQty`、`stationDefault` |

按角色裁剪价格：业务员得 `retailPrice` + `salesmanMinPrice`（**不下发分销价**）；
水站得 `wholesalePrice`；管理员得全量。

### 4.3 订单（§21.4）

| 方法 | 路径                 | 说明                                          |
| ---- | -------------------- | --------------------------------------------- |
| POST | `/orders`            | 下单（**必须带 `clientRequestId`**，见 §6.2） |
| GET  | `/orders`            | `?status&page&pageSize`；主体条件来自令牌     |
| GET  | `/orders/:id`        | §32 的 16 项 + `minPriceCheck`                |
| POST | `/orders/:id/cancel` | 取消（**同时全额退款到钱包**，见 §6.4）       |
| POST | `/orders/:id/refund` | 退款（已发货/已完成路径）                     |

`POST /orders` 请求体：

```jsonc
{
  "clientRequestId": "SALESMAN:SM0011758...a1b2c3", // 幂等键，≤64，客户端生成
  "fulfillmentType": "DELIVERY", // ⚠️ 白名单，只接受 DELIVERY（自提已下线）
  "orderScene": "CUSTOMER_ORDER", // 仅业务员：SELF_PURCHASE | CUSTOMER_ORDER
  "customerName": "张三",
  "customerPhone": "13911112222",
  "customerAddress": "南京市…", // **必填**（自提下线后无免地址场景）
  "addressSource": "STATION_DEFAULT", // 仅水站：STATION_DEFAULT | ORDER_CUSTOM
  "remark": "下午送",
  "items": [
    {
      "productId": "Pmrf…",
      "quantity": 5,
      "unitPrice": 20, // 仅业务员可传；水站传了也会被服务端忽略（§9.1）
      "useTicket": true,
      "ticketQty": 3
    } // 仅水站
  ]
}
```

### 4.4 钱包（§21.5 / §21.8）

| 方法 | 路径                                   | 鉴权             | 说明                                                                            |
| ---- | -------------------------------------- | ---------------- | ------------------------------------------------------------------------------- |
| GET  | `/wallet`                              | miniAuth         | 我的钱包概览（含 `byType` 分类汇总、`identityOk`）                              |
| GET  | `/wallet/transactions`                 | miniAuth         | `?type&page&pageSize`                                                           |
| POST | `/wallet/recharge`                     | miniAuth         | **Phase 6 未开通 → HTTP 503**                                                   |
| GET  | `/wallet/admin/overview`               | requireMiniAdmin | 全部钱包总览 + 合计                                                             |
| GET  | `/wallet/admin/:walletId/transactions` | requireMiniAdmin | 指定钱包流水 + 对账块                                                           |
| POST | `/wallet/admin/adjust`                 | requireMiniAdmin | `{ walletId, direction:'IN'\|'OUT', amount, reason, remark?, clientRequestId }` |

### 4.5 其他

| 方法 | 路径                     | 说明                                                           |
| ---- | ------------------------ | -------------------------------------------------------------- |
| GET  | `/home`                  | 按角色聚合首页（§28/§29/§30；全部服务端聚合）                  |
| GET  | `/customer-history`      | `?customerPhone&productId` —— **仅业务员**，只查自己的历史订单 |
| GET  | `/water-tickets`         | 我的水票（按商品 + 月份分组）—— **仅水站**                     |
| GET  | `/water-tickets/summary` | 水票汇总 —— 仅水站                                             |
| GET  | `/admin/dashboard`       | 管理员只读仪表盘                                               |

#### Phase 8b 管理员写操作 · 支出/费用域（第 1/17 域）

⚠️ 8b 每个域的**分层口径完全一致**（读 = `requireMiniAdmin`；写 = `requireMiniAdmin` + `requireMiniActive`；
写接口必须带幂等键，并在事务内落审计），详见 §5.8。

| 方法   | 路径                      | 鉴权                                     | 说明                                                                                                             |
| ------ | ------------------------- | ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| GET    | `/admin/expenses`         | `requireMiniAdmin`                       | 台账列表（`range` / `month` / `startDate·endDate` / `category` / `keyword` / 分页）；返回 `list/total/sumAmount` |
| GET    | `/admin/expenses/options` | `requireMiniAdmin`                       | 表单选项：`accounts`（仅**启用**账户 + 余额）、`presetCategories`、`customCategories`                            |
| GET    | `/admin/expenses/:id`     | `requireMiniAdmin`                       | 单条明细（编辑页回填，使编辑页可独立进入）                                                                       |
| POST   | `/admin/expenses`         | `requireMiniAdmin` + `requireMiniActive` | 新增；`{ expenseName, amount, expenseDate, category, accountId?, remark?, clientRequestId }`                     |
| PUT    | `/admin/expenses/:id`     | 同上                                     | 编辑（撤销旧流水 → 改记录 → 按新值记账，**净变化**而非叠加）                                                     |
| DELETE | `/admin/expenses/:id`     | 同上                                     | 删除（回补账户余额 + 清流水 + 审计）；幂等键可走 `?clientRequestId=`（DELETE 请求体并非所有客户端都保留）        |

---

## 五、关键设计决策（含与文档的差异，请评审确认）

### 5.1 自提已下线（2026-09-20 业务决定），原 `delivery_type = 3` 决策随之作废

**背景**：文档 §10.1.1 要求把「自提订单的 `delivery_type` 取什么值」在实现时定死，
本交付第一版据此决策为 **自提 → `3`（无需配送）**，并登记为待评审项。

**变更**：业务方决定**取消自提**，且要求**前后端一并移除**（不是只隐藏入口）。
因此上述决策不再是「已定死的取值」，而是**随功能一并作废** —— 该常量
（`constants/mini.js` 的 `PICKUP_DELIVERY_TYPE`）已删除。

移除范围与现存口径：

| 项              | 现状                                                                                           |
| --------------- | ---------------------------------------------------------------------------------------------- |
| 履约方式        | **只接受 `DELIVERY`**，白名单校验（非黑名单，变体拼写同样被拒）；缺失不默认放行                |
| 配送地址        | **每单必填**（原先「自提可免地址」的分支已删）                                                 |
| `delivery_type` | 只可能为 `1`（业务员/自有员工配送）或 `2`（水站配送）—— 不再产出「无需配送」的订单             |
| 履约状态        | `READY_FOR_PICKUP` **退役**（无代码路径可写入）                                                |
| 前端            | 无任何自提入口/字样/禁用的灰按钮；订单列表「待自提」页签、首页「待自提」统计与待办均已移除     |
| 接口契约        | `/config` 去掉 `pickupConfigured`、`/me` 去掉 `pickup`（契约冒烟**反向断言**它们不再出现）     |
| 配置            | `system_settings.mini_self_pickup_address` 已由迁移**删除**（留着会诱导管理员配置一个失效键）  |
| 列注释          | `orders.fulfillment_type` / `fulfillment_status` 的注释已更新（schema 自述不能宣传已下线能力） |
| 文档条款        | §10.4 / §44.14（自提相关）**不再适用**                                                         |

> 附带说明：`ProductSales.vue:197-199` 把 `3` 显示为「机台配送」，与列注释「无需配送」不一致。
> 该歧义与本次变更无关，也不由小程序裁决（小程序不再产生 3，展示走 `fulfillment_type`）。

> ⚠️ **恢复成本提示**：若将来要重新支持自提，不是「把常量加回来」那么简单 ——
> 需要同时恢复：白名单取值、地址免填分支、`READY_FOR_PICKUP` 状态与前端页签/统计、
> 自提地点配置键与读取服务（`miniSettings.js`，已在本次删除）、以及 §10.4/§44.14 的验收断言。
> 遗留的半状态（界面无入口、接口仍接受）最容易在恢复时被漏掉，故本次按「不留半状态」处理。

### 5.2 `wallet_transactions` 比文档多一个 `direction` 列

文档 §11.7 第 5 条记录了一个真实事故：**收入类流水的撤销是 `−amount`，抄成 `+` 会变成
「撤一次反而再加一笔」，而恒等式看起来仍然成立**。该坑的成因正是「用流水类型反推方向」。

因此本实现把方向**显式落库**：`amount` 恒为正数，方向只看 `direction`（1 入 / 2 出）；
撤销时取「**原方向的反向**」而不是按新类型再推一次。`reverseTransaction()` 的入参
刻意不接收 `direction`，从接口形状上堵住写反的可能。

### 5.3 取消与退款的优先级（文档 §16.1 要求实现时定死并回写本节）

| 路径     | 允许的履约状态                    | 结果                                                                                     |
| -------- | --------------------------------- | ---------------------------------------------------------------------------------------- |
| **取消** | `PAID` / `PROCESSING`（尚未发货） | 钱包全额退款 + `refund_status=REFUNDED` + `canceled_at` + 履约=`CANCELED`                |
| **退款** | `DELIVERING` / `COMPLETED`        | 钱包全额退款 + `refund_status=REFUNDED`，**履约状态保留退款前最后事实**（§16.1 第 3 条） |

> ⚠️ 2026-09-20 自提下线：退款允许状态中的 `READY_FOR_PICKUP` 已移除（该状态已退役）。

**为什么取消必然同时退款**：小程序订单是**下单即钱包支付**（§14.1），不存在「未支付的取消」，
所以取消与退款在小程序侧天然重合；Web 侧未支付订单的取消仍只走 `canceled_at`。

**库存 / 水票恢复条件**（§15.3「恢复库存（符合条件时）」的落地）：
当且仅当 `fulfillment_status !== COMPLETED` 时恢复。
已完成交付后退款属「已出货」，需实物退回后人工盘库，不能自动加回。

> ⚠️ 实现上**刻意不复用** `orderPricingService.restoreSalesEffects`：它内含「扣减水站
> `current_debt`」，而小程序水站订单从未增加欠款（§6.5.1），复用会让欠款变成负数。
> 库存恢复改用 `deductInventoryForSale(conn, productId, -qty)` —— 同样走行锁与存在性校验。

### 5.4 业务员成交价：前端可填 + 服务端强制下界（两处不是重复）

| 位置                                                   | 作用                                                         | 性质         |
| ------------------------------------------------------ | ------------------------------------------------------------ | ------------ |
| 前端 `order-confirm`（**成交价输入框**，`type=digit`） | 初值 = 参考零售价，可改；低于最低价时禁用提交按钮 + 红字说明 | **体验优化** |
| 服务端 `orderPricingService.assertSalesmanMinPrice`    | 事务内强制校验，不通过抛 bizFail → 400                       | **安全边界** |

**为什么前端必须有这个输入框**：Phase 3 明确要求「成交价（前端可填）」，确认订单页字段清单也含「成交价」。
缺少输入框时，最低价校验将退化为「拿档案零售价与下界比大小」——规则还在跑，但**保护对象没了**。
本期已补齐：默认值取 `retail_price`（§8.6 历史成交价「只作参考，不自动覆盖当前成交价」，
故不预填历史价），下界提示取 `salesman_min_price`。

前端改价后**立即重算**行小计、合计预览与红字校验，计算收敛在 `computePricing()` 一个纯函数里
（首次加载与用户改价复用同一段），避免出现「金额已变、红字还在」的不一致。

校验放在 `orderPricingService` 而非 `miniOrderController`：§42.3 明确要求，
且「手填价 → 回退零售价」的取值逻辑本来就在该文件里 —— 校验若在别处再推一遍，
两处一旦分叉就会出现「校验用 A、入库用 B」。

**§7.5 自购是否受约束 —— 业务方已确认（2026-09-20）：自购同样受约束。**
`order_scene`（自购 / 代客）**不影响**该校验；前端输入框与下界提示对两种场景完全一致，
服务端 `assertSalesmanMinPrice` 不接收 `order_scene` 参数、不按场景分支。
⚠️ 这条必须保持：若只为「代客下单」设限，任何低价单只要把场景切成「自己订货」即可绕过，规则即失效。
冒烟第 2 节对**两种场景各有一条断言**，并校验两者拒绝文案逐字一致 —— 只测一种的话，
将来若有人给自购加一条放行分支，现有断言仍会全绿。

### 5.5 购物车不存价格

`utils/cart.js` 只存 `productId` / `quantity` / 水票选择。若本地缓存价格，会出现
「加购时 18、下单时档案已改 20，用户看到的还是 18」—— 而且那是**前端算出来的金额**，
直接违反 §22.1 / §22.6。价格一律在下单页向后端取最新档案值。

确认订单页的「合计」标注为**预览**，并明示「最终以服务端计价为准」。

### 5.6 静态门禁（小程序侧没有编译期类型检查）

| 脚本                                          | 作用                                                                                                                                        |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `node miniprogram/scripts/check-project.js`   | 165 项：JSON 合法性 / 页面三件套 / tabBar ⊆ pages / **WXML 事件处理函数是否存在于同名 JS** / `wx:for` 是否带 `wx:key` / 标签闭合 / 密钥红线 |
| `node miniprogram/scripts/check-api-paths.js` | 小程序调用路径 ↔ 后端路由表交叉比对；当前 24 条全部命中，0 条后端路由未被调用                                                               |

第 4 项针对的是最隐蔽的一类错误：`bindtap="save"` 而 JS 里没有 `save` ——
开发者工具不报错，用户点下去没反应。

> 两个脚本都会**主动排除 WXML 注释**再扫描（否则注释里写的示例代码会被误报）。
> 门禁的构造性误报会训练人忽略告警，为此宁可在检查器里多写几行。

### 5.7 与文档的**有意偏离 / 范围收缩**

| 项                       | 文档                            | 本实现                              | 原因                                                                  |
| ------------------------ | ------------------------------- | ----------------------------------- | --------------------------------------------------------------------- |
| `wallet_transactions` 列 | §11.3 未含 `direction`          | 增加 `direction`                    | 见 §5.2（防方向写反）                                                 |
| **自提（配送方式之一）** | §10.1 / §10.4 / §44.14 要求支持 | **整体移除**（2026-09-20 业务决定） | 见 §5.1；含后端白名单拒绝、前端入口与统计页签移除、配置键与列注释清理 |

其余字段名、枚举值、状态机、接口路径均与文档一致；差异仅上述两处 ——
一处是按需扩展（`direction`），一处是**业务决定的范围收缩（自提下线）**。

### 5.8 Phase 8b 的「域定式」（后续 16 个域照此办理）

文档 §43 要求 8b「每完成一个业务域即验收一个」。但**逐域验收不等于逐域换写法** ——
否则 17 个域会分裂出 17 套鉴权/幂等/审计写法，评审时无法用同一套眼睛看。故先立定式：

| #   | 约定                                                                                            | 原因                                                                                       |
| --- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| ①   | 读接口挂 `requireMiniAdmin`；**写**接口再加 `requireMiniActive`                                 | 前者校验「是不是管理员」，后者校验「账号/主体是否被禁用」；管理员账号同样会被禁用（§22.5） |
| ②   | **不复用 Web 的 `requireAdmin`**                                                                | §22.4：小程序令牌若与 Web 共用校验，管理员身份等于拿到整本 Web 后台                        |
| ③   | 写接口**必须带幂等键**（`clientRequestId`）                                                     | 移动端弱网重试极常见；没有幂等键，一次重试就是两笔账，且两笔都合法、账面看不出异常         |
| ④   | 幂等指纹走 `utils/requestHash.js`（**哈希后落库**）                                             | `mini_idempotency.request_hash` 是 `varchar(64)`；拼明文串会直接 500（2026-09-21 实测）    |
| ⑤   | 资金动作调**既有账务原语**（`applyExpenseLedger` / `revertExpenseLedger` 等），域控制器只做编排 | §41 头号禁止项：不复制余额与流水的写法；副作用是两端记账方向**物理上不可能分叉**           |
| ⑥   | 每次写操作**在事务内**落审计（§40），`actor_id = mini:<accountId>`                              | 与 Web 端操作可区分；审计与业务同事务，避免「操作成功但没痕迹」                            |
| ⑦   | 列表筛选复用既有筛选构造（如 `buildExpenseListWhere`）                                          | 本仓库有「start 含 / end 不含」与「end 含」**两套区间约定**，另写一遍极可能挑错            |
| ⑧   | 分页 `parseInt` 后内联；下拉一律走专用全量接口                                                  | mysql2 不支持 `LIMIT ?`；分页拉下拉会静默缺项                                              |

---

## 六、验证

### 6.1 后端冒烟

```bash
cd backend
node scripts/smoke_mini_program.js            # 122 项：业务规则 + 资金纪律 + 令牌隔离
node scripts/smoke_mini_frontend_contract.js  #  91 项：页面视角的接口契约
node scripts/smoke_mini_admin.js              #  53 项：Phase 8b 管理端写操作（支出域）
node scripts/smoke_expense_list.js            #  27 项：Web 端支出台账列表筛选分支（只读）
```

> ⚠️ 跑之前**必须重启后端**（否则测的是旧代码），且两次跑之间无需清库 —— 两个脚本都可重入
> （实测连跑两遍结果一致）。

`smoke_mini_program.js` 覆盖的验收条款：

| 条款                        | 覆盖内容                                                                                                                                                                                                                              |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| §44.11 ① ② ③                | 小程序令牌访问 Web 接口被拒 / 小程序 ADMIN 令牌访问 Web 管理接口被拒 / Web 令牌访问小程序接口被拒                                                                                                                                     |
| §44.11 ④ ⑤                  | 禁用账号持**未过期**令牌下单/充值被拒（HTTP 401）；绑定主体停用同样被拒                                                                                                                                                               |
| §44.11 ⑥                    | 解绑后唯一键释放、新账号可立即绑定；旧账号无法同时恢复启用                                                                                                                                                                            |
| §44.12 ①②③④⑤                | 钱包恒等式（含停用钱包）/ 收入类流水撤销方向 / 重复取消不重复加积分 / 两套账本同事务                                                                                                                                                  |
| §44.13                      | 迁移已拆单件值与总额列，存量回填无偏差（Phase 7 未接入，故只验列与回填）                                                                                                                                                              |
| §44.14                      | ⚠️ **自提部分（① ② ④）已随自提下线作废**；第 7 节改为验证「自提已下线：构造 `PICKUP` 及其变体 `SELF_PICKUP` 被拒 400、缺失 `fulfillmentType` 不被默认放行、配送单缺地址被拒」；原 ③（水站地址变更后历史订单快照不变）与自提无关，保留 |
| §44.2 / §7.5 / §8.4 / §44.4 | 最低成交价**下界**（等于放行、低于拒绝）+ **上界**（高于参考零售价放行且被采用，第 14 节）/ **自购与代客同样受约束（两条断言 + 文案一致 + 拒单不留痕）** / 水站订单 `current_debt` 不变                                               |
| §23.1 / §45                 | 同键重复提交返回同一订单且不再扣款 / 同键不同参数 400 / 退款幂等 / 管理员重复点击幂等                                                                                                                                                 |

### 6.2 小程序静态校验

```bash
node miniprogram/scripts/check-project.js     # 165 项
node miniprogram/scripts/check-api-paths.js   # 24 条路径交叉比对
```

### 6.3 回归（确认未影响既有功能）

```bash
cd backend
node scripts/smoke_order_pricing.js           # 48/48
node scripts/smoke_order_revenue_posting.js   # 25/25
node scripts/smoke_role_auth.js               # 10/10
node scripts/smoke_normalize_body.js          # 26/26
node scripts/smoke_masterdata_delete.js       # 44/44
```

### 6.4 数据库迁移

```bash
cd backend
node scripts/migrate_mini_program_v1.js --dry-run   # 预演
node scripts/migrate_mini_program_v1.js --apply     # 执行（可反复重跑）
```

---

## 七、已知缺口与后续建议

### 7.1 Phase 6（微信充值）未做

`POST /api/mini/wallet/recharge` 返回 **HTTP 503 + 明确文案**，收费页面如实说明状态并给出替代路径。

**为什么不放一个「模拟充值」按钮**：§41 禁止把不存在的资金能力做成看起来可用的入口；
§13.8 明确「降级期间不要造假数据（不得写测试用的『已充值』流水进生产库）」。
伪造的成功提示会让账实不符且无人察觉 —— 这正是本仓库历史上踩过的坑。

**接入时需要**：`WX_APPID` / `WX_MCHID` / `WX_API_V3_KEY` / `WX_PRIVATE_KEY` / `WX_CERT_SERIAL_NO`（仅服务端）

- 微信支付 v3 下单与回调验签 + 复用 `mini_payment_orders`（表与 `out_trade_no` 唯一索引已就位，
  `credited` 标记即 §13.6 的幂等落点）。

### 7.2 Phase 7（水票积分）未做

- 数据库已按 §20.4 补齐 `distribution_delivery_fee_unit` / `_total` / `wallet_transaction_id` 并对存量回填；
- **未改动** `waterTicketController`：该阶段会改动既有 Web 端点（§12.9.1 列了 5 个：
  `/adjust-delivery-fee`、`/adjust-station-delivery-fee`、`PUT /issuances/:id`、
  `/adjust-balance`、`DELETE /issuances/batch/:batchId`），文档要求逐个定处置方案 + 准备回滚路径，
  属独立批次。
- 接入时必须落实 §12.10：`unit_distribution_fee` **一律由服务端从 `products.distribution_delivery_fee` 重取**，
  前端传入值仅作展示（否则等于把「公司欠水站多少积分」的决定权交给客户端）。

### 7.3 Phase 8b（管理员写操作）🔶 进行中 1/17

按 §5.3.1 / §43，8b **逐域验收**（不等 17 个一起验），每个写域的定式见 §5.8。

| #    | 业务域                                                                                                                          | 状态          | 说明                                                                                                |
| ---- | ------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------- |
| 1    | **支出/费用**                                                                                                                   | ✅ **已完成** | 列表/明细/新增/编辑/删除 6 个接口 + 2 个页面 + `smoke_mini_admin.js` 53 项；账户可留空=只登记不动账 |
| 2–17 | 商品 / 库存 / 订单 / 水站 / 供应商 / 员工与业务员 / 机台 / 水票 / 回桶 / 公司账户 / 收入 / 工资 / 营收 / 成本 / 利润 / 系统设置 | ⬜ 未做       | 仍走 Web 管理端；入口**不占位、不放灰按钮**（§41 禁止把不存在的能力做成可用入口）                   |

> ✅ **顺带补齐一个既有覆盖缺口**：Web 端 `GET /api/expenses` 此前**没有冒烟覆盖**，
> 而本次为 8b 抽出了它的筛选构造 `buildExpenseListWhere`（两端共用）—— 也就是说这次重构
> 一开始是在**没有防线**的情况下做的。已补 `backend/scripts/smoke_expense_list.js`（**27 项**，
> 只读）：覆盖 range 预设 / month 旧参数兼容 / 无 range 的闭区间分支 / 非法区间必须 400
> （不得静默降级为「不限区间」）/ category·keyword / 分页与越界页码。

### 7.4 其他待办

| 项                    | 说明                                                                                                                      |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| 小程序账号管理 UI     | 禁用 / 解绑 / 改角色目前需 SQL 或等 8b。服务端的每请求校验已生效（禁用即时生效已验证）                                    |
| `API_ORIGIN` 改 https | 正式发布前必须改为已在小程序后台备案的域名（当前 `http://localhost:3000` 仅供开发者工具调试）                             |
| §7.5 业务确认         | ✅ **已确认（2026-09-20）：自购同样受最低成交价约束**，不再按假设实现；代码注释、两种场景的断言、本节均已回写。**已关闭** |
| 发货管理              | 若经营类目触发「交易类小程序发货管理」，需以 `fulfillment_status` 为基础做客侧回传（§25.1 第 4 条，工作量未评估）         |
| 并发压测              | 文档 §45 的「库存并发 / 钱包并发扣款 / 水票并发核销」未纳入冒烟（需并发压测工具），建议上线前单独跑一轮                   |

### 7.5 提交状态

✅ **已提交并推送**：`8a6db77`（111 文件，+15286 / −316），远端 `origin/master` 已同步。
下一批开发继续沿用同一流程（红线扫描 → 提交 → 推送，推送走 API 通道，见 `.workbuddy/memory/MEMORY.md` 门禁章节）。

---

## 八、数据安全与合规提示

- **密钥**：`WX_*` / `JWT_SECRET_MINI` 只存服务端 `backend/.env`，**未进入小程序包**
  （`check-project.js` 有对应的红线检查）。小程序侧只有 `API_ORIGIN` 一个常量。
- **个人信息**：接口一律不下发明文手机号（`/me` 与商品详情的 `stationDefault.phoneMasked` 均为脱敏态），
  令牌 payload 不含 openid；「使用水站默认地址」下单时联系人电话由**服务端**从水站档案取，不经过客户端。
- **⚠️ 既有风险未扩大**：`database/full_schema_data.sql` 含真实 PII 且仓库公开 —— 该问题已登记为
  「已知并接受的风险」。本次新增的 5 张表均为**空表**（迁移不写入任何种子数据），未扩大这个面。
